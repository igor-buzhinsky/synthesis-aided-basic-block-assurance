MODULE main
VAR
    DELAY_INPUT_SIGN : boolean;
    DELAY_UP : 0..2;
    DELAY_DOWN : 0..2;
    block : BINARY_DELAY(DELAY_INPUT_SIGN, DELAY_UP, DELAY_DOWN);
DEFINE
    DELAY_OUTPUT_SIGN := block.DELAY_OUTPUT_SIGN;
    DELAY_OUTPUT_SIGN_FAULT := block.DELAY_OUTPUT_SIGN_FAULT;
